# Multisensor ML — Goal 1.5

Oracle/sanity-only machine-learning pipeline for the synthetic truth produced by
`../multisensor_synth`. It does not claim real-device, medical, or Goal 2–4
`model_ready` performance.
